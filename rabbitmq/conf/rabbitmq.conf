# RabbitMQ para laboratorio Cloud Native.
# Las credenciales se entregan por variables de entorno:
# RABBITMQ_DEFAULT_USER y RABBITMQ_DEFAULT_PASS.

listeners.tcp.default = 5672
management.tcp.port = 15672

# Para laboratorio se dejan logs en stdout/stderr del contenedor.
log.console = true
